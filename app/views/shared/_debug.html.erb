<% if Rails.application.secrets.debug.present? %>
  <div class="debug">
    <%= controller.class.name %>
    | app <%= current_or_new_snap_application.id %>
    | <%= link_to 'Nav', steps_path %>
  </div>
<% end %>
