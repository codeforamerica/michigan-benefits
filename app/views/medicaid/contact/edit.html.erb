<% content_for :header_title, "Contact Information & Followup" %>

<div class="form-card">
  <header class="form-card__header">
    <div class="form-card__title">
      Do you have stable housing right now?
    </div>
  </header>

  <%= render 'shared/yes_no_form',
    step: @step,
   field: :stable_housing %>
</div>
