<% content_for :header_title, "Quick Tax Questions" %>

<div class="form-card">
  <header class="form-card__header">
    <div class="form-card__title">
      Are you claimed as a dependent on anyone else's taxes?
    </div>
  </header>

  <%= render 'shared/yes_no_form',
    step: @step,
    field: :claimed_as_dependent %>
</div>
