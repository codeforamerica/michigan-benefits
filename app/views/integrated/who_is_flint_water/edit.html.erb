<% content_for :header_title, "Flint Water Crisis" %>
<% content_for :form_card_title, "Who has been affected by the Flint Water Crisis?" %>

<% content_for :form_card_body do %>
  <%= fields_for(:form, @form, builder: MbFormBuilder) do |f| %>
    <fieldset class="input-group">
      <legend class="sr-only">
        Choose who is flint_water.
      </legend>
      <% @form.members.each do |member| %>
        <%= f.fields_for('members[]', member, hidden_field_id: true) do |ff| %>
          <%= ff.mb_checkbox :flint_water,
                             member.display_name,
                             options: {
                               checked_value: "yes",
                               unchecked_value: "no",
                             } %>
        <% end %>
      <% end %>
      <%= f.mb_form_errors :members %>
    </fieldset>
  <% end %>
<% end %>
