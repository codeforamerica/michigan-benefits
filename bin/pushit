#!/usr/bin/env ruby
require_relative "bin_helpers"

chdir APP_ROOT do
  system! "bin/update"

  step "Running tests" do
    system! "QUIET_TESTS=true bin/rails spec"
  end

  step "Pushing" do
    system! "git push"
  end
end
